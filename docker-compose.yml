services:
  zeroclaw:
    image: ghcr.io/dizys/my-zeroclaw:latest
    restart: unless-stopped
    ports:
      - "42617:42617"
    volumes:
      - zeroclaw-data:/data
    environment:
      - ZEROCLAW_WORKSPACE=/data/workspace

volumes:
  zeroclaw-data:
